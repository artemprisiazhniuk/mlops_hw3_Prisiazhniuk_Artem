services:
  nginx:
    image: nginx:latest
    ports:
      - "8086:80"
    volumes:
      - ./nginx/:/etc/nginx:ro
    depends_on:
      - ml-service-blue
      - ml-service-green

networks:
  default:
    name: demo-net